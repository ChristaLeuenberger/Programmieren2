package calculations;

public class DistanceCalculator {
    public DistanceCalculator() {
    }

    public void calculate(Coordinates from, Coordinates to, String resolution) {
        double fromX = from.getX();
        double fromY = from.getY();
        double toX = to.getX();
        double toY = to.getY();
    }
}
